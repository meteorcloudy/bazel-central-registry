name: Notify Module Maintainers
on:
  pull_request:
    paths:
      - 'modules/**'

permissions:
  pull-requests: write

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@eb238b55efaa70779f274895e782ed17c84f2895 # v2.6.1
        with:
          egress-policy: audit

      - name: Run BCR helper
        uses: meteorcloudy/continuous-integration/actions/bcr-helper@efd26a4f015225d7bd46cca26f7fb7401a246cb8 # master
        with:
          token: ${{ secrets.TEST_BCR_TOKEN }}
